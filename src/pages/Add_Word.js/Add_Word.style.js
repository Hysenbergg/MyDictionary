import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        margin: 10
    },
    title: {
        fontSize: 24,
        color: 'black',
        textAlign: 'center',
        margin: 24,
        fontWeight: 'bold'
    },
})