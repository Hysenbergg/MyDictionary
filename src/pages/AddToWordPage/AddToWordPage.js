import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const AddToWordPage = () => {
    return(
        <View style={styles.container}>
            <Text> AddToWordPage </Text>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
})

export default AddToWordPage;