import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    title: {
        flex: 0.3,
        fontSize: 50,
        fontWeight: "bold",
        color: 'orange',
        padding: 15,
        marginTop: 50,
    },
    buttons_container: {
        flex: 0.7,
    },
})